using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlPlaticasRefugio : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //PlayerPrefs.DeleteAll();
        /*
        PlayerPrefs.SetInt("platicaConMama1",0);
        PlayerPrefs.Save();
        PlayerPrefs.SetInt("platicaConMama2",0);
        PlayerPrefs.Save();
        PlayerPrefs.SetInt("platicaConIngeniero",0);
        PlayerPrefs.Save();
        */
    }
}
